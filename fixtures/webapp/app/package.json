{
  "name": "flight-ops-webapp",
  "version": "1.0.0",
  "private": true,
  "description": "Runnable fullstack fixture — Northwind Airlines flight-ops console. Zero runtime deps; built-in http + node:test.",
  "type": "commonjs",
  "engines": { "node": ">=18" },
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test",
    "build": "node scripts/build.js"
  },
  "dependencies": {}
}
